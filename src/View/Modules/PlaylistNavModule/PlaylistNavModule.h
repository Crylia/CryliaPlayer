#pragma once

#include <QWidget>
#include <QFrame>
#include <QLayout>

class PlaylistNavModule : public QFrame {

  Q_OBJECT

private:

public:
  PlaylistNavModule(QWidget* parent);
  ~PlaylistNavModule( );
};
